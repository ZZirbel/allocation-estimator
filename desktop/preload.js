// Preload script — sandboxed bridge between Electron and the web app
// Currently empty since the app runs as a standard web page.
// Can be extended for native OS integrations (file dialogs, notifications, etc.)
